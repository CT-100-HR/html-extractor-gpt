services:
  - type: web
    name: html-extractor-gpt
    env: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    region: oregon
    branch: main
    runtime: node
    autoDeploy: true
